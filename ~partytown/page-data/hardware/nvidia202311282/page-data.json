{"componentChunkName":"component---src-pages-hardware-nvidia-202311282-mdx","path":"/hardware/nvidia202311282/","result":{"pageContext":{"frontmatter":{"description":"Hardware / Nvidia 202311282","title":"Hardware / Nvidia 202311282"},"relativePagePath":"/hardware/nvidia202311282.mdx","titleType":"page","MdxNode":{"id":"c4f3b538-ca99-5a12-864c-80a21ba27f6f","children":[],"parent":"85a1c17d-510f-54e8-aeaa-a05bf1b5dcec","internal":{"content":"import HomepageTemplate from './Template.js';\nexport default HomepageTemplate;\n\n<br></br><br></br>\n\n<span style=\"color: #3256A6; font-size: 20px;\">本笔记内容来自YouTube视频 \"CUDA Part B_ New Features in CUDA 5 on Kepler\" by Peter Messmer</span>\n\n# <span style=\"color: #8E8E93; font-weight: bold\">5到5.5的改进</span>\n\n1. Performance\n2. Efficiency\n3. Programmability\n\n早期CUDA只作为计算库编写并没有同时兼顾第一点和第三点，这些问题在后续得到了改进\n\n## Libaray Calls With Kernel\n\nLibaray calls from kernels，早期不支持内核级系统调用，只支持用__device__关键字将函数标记\n\nSimpify GPU / CPU devide，整合二者之间分开的地址空间（zero-copy使得cpu和gpu共享相同的物理内存，使用了unified virtual memory）\n\n```cpp\n#include <iostream>\n\n__global__ void kernel(int* data) {\n    int tid = threadIdx.x;\n    data[tid] = data[tid] * 2;  // 在GPU上执行操作，修改数据\n}\n\nint main() {\n    const int arraySize = 256;\n    int* hostData;\n    int* deviceData;\n\n    // 在主机上分配内存\n    hostData = new int[arraySize];\n\n    // 在设备上分配内存，这里使用cudaMallocManaged分配UVM内存\n    cudaMallocManaged(&deviceData, arraySize * sizeof(int));\n\n    // 初始化数据\n    for (int i = 0; i < arraySize; i++) {\n        hostData[i] = i;\n    }\n\n    // 不需要显式cudaMemcpy\n    kernel<<<1, arraySize>>>(deviceData);\n    cudaDeviceSynchronize();\n\n    // 打印修改后的数据\n    for (int i = 0; i < arraySize; i++) {\n        std::cout << deviceData[i] << \" \";\n    }\n\n    // 释放内存\n    delete[] hostData;\n    cudaFree(deviceData);\n\n    return 0;\n}\n```\n\n## Dynamic Parallelism\n\n参见这个slide：https://www.cs.ucr.edu/~amazl001/teaching/cs147/S21/slides/14-DynamicParallelism.pdf\n\n根据第四页所显示的，当gpu不能自产生任务情况，会加重PCI express bus的传输。\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/e.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/f.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\n第十一页中模拟了一个尾流的计算情况，众所周知，头部的渲染实际上是比较容易预测的， 但是没有Dynamic Parallelism的显卡不具备将重要任务和次要任务分配不同资源的能力。\n\n一些更具体的例子可以看这个视频：https://www.youtube.com/watch?v=X74BLPO8tT4\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/g.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/h.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\ncuda允许我们在父kernel里调用子kernel就是Dynamic Parallelism的体现，在开普勒系列之前，子kernel的调用要参照上述图所写的形式（参考费米系列）\n\n另外一种更明显的例子就是在面对序列化的数据时、或非uniform的邻接矩阵/链表时\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/i.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/j.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n很明显右图的例子是不可取的，我们应该使用左图这种方式。\n\n## Hyper-Q 和 Grid Management Unit\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/k.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\n上图为GK110新增的硬件处理单元“Grid Management Unit”，Grid Management Unit的存在是动态并行机制的核心，因为SMX会生成一些cuda generated works（kernel）返回GMU内，在pending grid中等待，而左图旧版本的GPU缺乏生成子任务并返回调度的机制。\n\n参考材料[Hyper Q Sample](https://developer.download.nvidia.com/compute/DevZone/C/html_x64/6_Advanced/simpleHyperQ/doc/HyperQ.pdf) 所给出的例子，Hyper-Q和GMU诞生的原因并非CPU产生的任务序列们不能并行的执行在GPU，而是有另外一种意想不到的情况发生：\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/l.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/m.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n如图所示，A-B-C是一个需要有序执行的、原本上可以在GPU并行的大任务序列，CPU产生了这个任务，并将其分割成了A0-B0-C0、A1-B1-C1、A2-B2-C2，三个可供GPU并行执行的小任务，但是硬件并不能正确的理解三者并行的概念。\n\n硬件实际只能并行执行C0-A1和C1-A2，因为硬件只能识别两组子任务的交界处来判断这是两组不同的子任务。\n\n为了验证这个猜想，以下用Visual Profiler来模拟：\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/n.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/o.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\n可以看到，在实际操作中，不加GMU的GPU确实没有办法理解整体并行的概念，而是只能理解交界处的任务可以共同执行，加了GMU可以做到真正的并行。我们不得而知其中的硬件奥秘，即：如何错误的识别和如何让其恢复正确的识别，我们只能得知Nvidia的确采用了某种机制来确保dynamic parallelism。\n\n\n## Atomic 和 Shuffle\n\n### shuffle例子\n\n```cpp\n#include <cuda_runtime.h>\n#include <device_launch_parameters.h>\n\n__global__ void shflExample(int *data) {\n    int tid = threadIdx.x;\n    int myValue = data[tid];\n\n    // 使用 shfl 进行数据交换，计算累积和\n    for (int offset = 1; offset < blockDim.x; offset *= 2) {\n        int receivedValue = __shfl_sync(0xFFFFFFFF, myValue, tid - offset);\n\n        if (tid >= offset) {\n            myValue += receivedValue;\n        }\n    }\n\n    // 在这里，myValue 包含了线程在数组中前面值的累积和\n\n    // 其他的计算和处理...\n}\n\nint main() {\n    // 启动 CUDA 核函数\n    int dataSize = 32;\n    int *d_data;\n    cudaMalloc((void**)&d_data, dataSize * sizeof(int));\n\n    shflExample<<<1, dataSize>>>(d_data);\n\n    cudaFree(d_data);\n\n    return 0;\n}\n```\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/p.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n这个例子展示如何在线程束内计算累积和。假设你有一个线程束中的一组值，想要计算每个线程在数组中前面值的累积和，每个线程使用 `__shfl_sync` 从其前面的线程获取值，并将其加到自己的值上。通过循环，每个线程最终都会包含数组中前面所有值的累积和。（此例子为chatgpt提供）\n\n### atomic例子：\n\n在早期计算多个数字的总和时需要有两次kernel launch（计算partial sum和计算所有partial sum的sum），使用atomic做一次kernel launch就可以\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/q.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/r.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n","type":"Mdx","contentDigest":"3eb96a87984754ba80aa849fc2e105ab","owner":"gatsby-plugin-mdx","counter":114},"frontmatter":{"description":"Hardware / Nvidia 202311282","title":"Hardware / Nvidia 202311282"},"exports":{},"rawBody":"import HomepageTemplate from './Template.js';\nexport default HomepageTemplate;\n\n<br></br><br></br>\n\n<span style=\"color: #3256A6; font-size: 20px;\">本笔记内容来自YouTube视频 \"CUDA Part B_ New Features in CUDA 5 on Kepler\" by Peter Messmer</span>\n\n# <span style=\"color: #8E8E93; font-weight: bold\">5到5.5的改进</span>\n\n1. Performance\n2. Efficiency\n3. Programmability\n\n早期CUDA只作为计算库编写并没有同时兼顾第一点和第三点，这些问题在后续得到了改进\n\n## Libaray Calls With Kernel\n\nLibaray calls from kernels，早期不支持内核级系统调用，只支持用__device__关键字将函数标记\n\nSimpify GPU / CPU devide，整合二者之间分开的地址空间（zero-copy使得cpu和gpu共享相同的物理内存，使用了unified virtual memory）\n\n```cpp\n#include <iostream>\n\n__global__ void kernel(int* data) {\n    int tid = threadIdx.x;\n    data[tid] = data[tid] * 2;  // 在GPU上执行操作，修改数据\n}\n\nint main() {\n    const int arraySize = 256;\n    int* hostData;\n    int* deviceData;\n\n    // 在主机上分配内存\n    hostData = new int[arraySize];\n\n    // 在设备上分配内存，这里使用cudaMallocManaged分配UVM内存\n    cudaMallocManaged(&deviceData, arraySize * sizeof(int));\n\n    // 初始化数据\n    for (int i = 0; i < arraySize; i++) {\n        hostData[i] = i;\n    }\n\n    // 不需要显式cudaMemcpy\n    kernel<<<1, arraySize>>>(deviceData);\n    cudaDeviceSynchronize();\n\n    // 打印修改后的数据\n    for (int i = 0; i < arraySize; i++) {\n        std::cout << deviceData[i] << \" \";\n    }\n\n    // 释放内存\n    delete[] hostData;\n    cudaFree(deviceData);\n\n    return 0;\n}\n```\n\n## Dynamic Parallelism\n\n参见这个slide：https://www.cs.ucr.edu/~amazl001/teaching/cs147/S21/slides/14-DynamicParallelism.pdf\n\n根据第四页所显示的，当gpu不能自产生任务情况，会加重PCI express bus的传输。\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/e.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/f.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\n第十一页中模拟了一个尾流的计算情况，众所周知，头部的渲染实际上是比较容易预测的， 但是没有Dynamic Parallelism的显卡不具备将重要任务和次要任务分配不同资源的能力。\n\n一些更具体的例子可以看这个视频：https://www.youtube.com/watch?v=X74BLPO8tT4\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/g.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/h.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\ncuda允许我们在父kernel里调用子kernel就是Dynamic Parallelism的体现，在开普勒系列之前，子kernel的调用要参照上述图所写的形式（参考费米系列）\n\n另外一种更明显的例子就是在面对序列化的数据时、或非uniform的邻接矩阵/链表时\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/i.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/j.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n很明显右图的例子是不可取的，我们应该使用左图这种方式。\n\n## Hyper-Q 和 Grid Management Unit\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/k.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\n上图为GK110新增的硬件处理单元“Grid Management Unit”，Grid Management Unit的存在是动态并行机制的核心，因为SMX会生成一些cuda generated works（kernel）返回GMU内，在pending grid中等待，而左图旧版本的GPU缺乏生成子任务并返回调度的机制。\n\n参考材料[Hyper Q Sample](https://developer.download.nvidia.com/compute/DevZone/C/html_x64/6_Advanced/simpleHyperQ/doc/HyperQ.pdf) 所给出的例子，Hyper-Q和GMU诞生的原因并非CPU产生的任务序列们不能并行的执行在GPU，而是有另外一种意想不到的情况发生：\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/l.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/m.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n如图所示，A-B-C是一个需要有序执行的、原本上可以在GPU并行的大任务序列，CPU产生了这个任务，并将其分割成了A0-B0-C0、A1-B1-C1、A2-B2-C2，三个可供GPU并行执行的小任务，但是硬件并不能正确的理解三者并行的概念。\n\n硬件实际只能并行执行C0-A1和C1-A2，因为硬件只能识别两组子任务的交界处来判断这是两组不同的子任务。\n\n为了验证这个猜想，以下用Visual Profiler来模拟：\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/n.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/o.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\n可以看到，在实际操作中，不加GMU的GPU确实没有办法理解整体并行的概念，而是只能理解交界处的任务可以共同执行，加了GMU可以做到真正的并行。我们不得而知其中的硬件奥秘，即：如何错误的识别和如何让其恢复正确的识别，我们只能得知Nvidia的确采用了某种机制来确保dynamic parallelism。\n\n\n## Atomic 和 Shuffle\n\n### shuffle例子\n\n```cpp\n#include <cuda_runtime.h>\n#include <device_launch_parameters.h>\n\n__global__ void shflExample(int *data) {\n    int tid = threadIdx.x;\n    int myValue = data[tid];\n\n    // 使用 shfl 进行数据交换，计算累积和\n    for (int offset = 1; offset < blockDim.x; offset *= 2) {\n        int receivedValue = __shfl_sync(0xFFFFFFFF, myValue, tid - offset);\n\n        if (tid >= offset) {\n            myValue += receivedValue;\n        }\n    }\n\n    // 在这里，myValue 包含了线程在数组中前面值的累积和\n\n    // 其他的计算和处理...\n}\n\nint main() {\n    // 启动 CUDA 核函数\n    int dataSize = 32;\n    int *d_data;\n    cudaMalloc((void**)&d_data, dataSize * sizeof(int));\n\n    shflExample<<<1, dataSize>>>(d_data);\n\n    cudaFree(d_data);\n\n    return 0;\n}\n```\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/p.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n这个例子展示如何在线程束内计算累积和。假设你有一个线程束中的一组值，想要计算每个线程在数组中前面值的累积和，每个线程使用 `__shfl_sync` 从其前面的线程获取值，并将其加到自己的值上。通过循环，每个线程最终都会包含数组中前面所有值的累积和。（此例子为chatgpt提供）\n\n### atomic例子：\n\n在早期计算多个数字的总和时需要有两次kernel launch（计算partial sum和计算所有partial sum的sum），使用atomic做一次kernel launch就可以\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/q.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](/r.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n","fileAbsolutePath":"/init/dev/packages/example/src/pages/hardware/nvidia202311282.mdx"}}},"staticQueryHashes":["1364590287","137577622","2102389209","2456312558","2746626797","3018647132","3037994772","768070550"]}