{
    "componentChunkName": "component---src-pages-nvidia-cuda-55-partb-1-index-mdx",
    "path": "/nvidia/cuda55partb1/",
    "result": {"pageContext":{"frontmatter":{"description":"Nvidia / Cuda 55 Partb 1","title":"Nvidia / Cuda 55 Partb 1"},"relativePagePath":"/nvidia/cuda55partb1/index.mdx","titleType":"page","MdxNode":{"id":"7aae517b-edda-5d88-8491-e646ae3b66e7","children":[],"parent":"5682e0eb-91a6-590f-afe6-fb24d85a8801","internal":{"content":"import HomepageTemplate from \"./Blogpage.js\";\nexport default HomepageTemplate;\n\n\n## <span style={{ fontWeight: 'bold', color: '#0047ab' }}>Last Updated: 10/22/2023</span>\n\n<PageDescription>\n  <span style={{ fontSize: \"1.8rem\", color: \"#ffffff\", fontWeight: \"light\" }}>\n    本笔记内容来自YouTube视频 \"CUDA Part B_ New Features in CUDA 5 on Kepler; Peter Messmer (NVIDIA)\"\n    <br />\n    感兴趣的人可以自行搜索相关内容。\n    <br />\n    Part B这个部分有点难，分成B1，B2更新（未完）。\n  </span>\n</PageDescription>\n\n\n\n5到5.5的改进\n\n- Performance\n- Efficiency\n- Programmability\n\n早期CUDA只作为计算库编写并没有同时兼顾第一点和第三点，这些问题在后续得到了改进\n\n### Libaray Calls With Kernel \n\n- Libaray calls from kernels，早期不支持内核级系统调用，只支持用`__device__`关键字将函数标记\n- Simpify GPU / CPU devide，整合二者之间分开的地址空间（zero-copy使得cpu和gpu共享相同的物理内存，使用了unified virtual memory）\n```cpp\n#include <iostream>\n\n__global__ void kernel(int* data) {\n    int tid = threadIdx.x;\n    data[tid] = data[tid] * 2;  // 在GPU上执行操作，修改数据\n}\n\nint main() {\n    const int arraySize = 256;\n    int* hostData;\n    int* deviceData;\n\n    // 在主机上分配内存\n    hostData = new int[arraySize];\n\n    // 在设备上分配内存，这里使用cudaMallocManaged分配UVM内存\n    cudaMallocManaged(&deviceData, arraySize * sizeof(int));\n\n    // 初始化数据\n    for (int i = 0; i < arraySize; i++) {\n        hostData[i] = i;\n    }\n\n    // 不需要显式cudaMemcpy\n    kernel<<<1, arraySize>>>(deviceData);\n    cudaDeviceSynchronize();\n\n    // 打印修改后的数据\n    for (int i = 0; i < arraySize; i++) {\n        std::cout << deviceData[i] << \" \";\n    }\n\n    // 释放内存\n    delete[] hostData;\n    cudaFree(deviceData);\n\n    return 0;\n}\n```\n### Dynamic Parallelism\n\n参见[这个slide](https://www.cs.ucr.edu/~amazl001/teaching/cs147/S21/slides/14-DynamicParallelism.pdf)\n\n根据第四页所显示的，当gpu不能自产生任务情况，会加重PCI express bus的传输。\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./c.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./d.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n第十一页中模拟了一个尾流的计算情况，众所周知，头部的渲染实际上是比较容易预测的，\n但是没有Dynamic Parallelism的显卡不具备将重要任务和次要任务分配不同资源的能力。\n\n一些更具体的例子可以看[这个视频](https://www.youtube.com/watch?v=X74BLPO8tT4)\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./e.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./f.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\ncuda允许我们在父kernel里调用子kernel就是Dynamic Parallelism的体现，在开普勒系列之前，子kernel的调用要参照上述图所写的形式（参考费米系列）\n\n另外一种更明显的例子就是在面对序列化的数据时、或非uniform的邻接矩阵/链表时\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./h.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./i.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n很明显右图的例子是不可取的，我们应该使用左图这种方式。\n\n### Hyper-Q 和 Grid Management Unit\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![第4页](./a1.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n上图为GK110新增的硬件处理单元“Grid Management Unit”，Grid Management Unit的存在是动态并行机制的核心，因为SMX会生成一些cuda generated works（kernel）返回GMU内，在pending grid中等待，而左图旧版本的GPU缺乏生成子任务并返回调度的机制。\n\n参考材料[Hyper Q Sample](https://developer.download.nvidia.com/compute/DevZone/C/html_x64/6_Advanced/simpleHyperQ/doc/HyperQ.pdf) 所给出的例子，Hyper-Q和GMU诞生的原因并非CPU产生的任务序列们不能并行的执行在GPU，而是有另外一种意想不到的情况发生：\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a4.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a3.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n如图所示，A-B-C是一个需要有序执行的、原本上可以在GPU并行的大任务序列，CPU产生了这个任务，并将其分割成了A0-B0-C0、A1-B1-C1、A2-B2-C2，三个可供GPU并行执行的小任务，但是硬件并不能正确的理解三者并行的概念。\n\n硬件实际只能并行执行C0-A1和C1-A2，因为硬件只能识别两组子任务的交界处来判断这是两组不同的子任务。\n\n为了验证这个猜想，以下用Visual Profiler来模拟：\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a5.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a2.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n可以看到，在实际操作中，不加GMU的GPU确实没有办法理解整体并行的概念，而是只能理解交界处的任务可以共同执行，加了GMU可以做到真正的并行。我们不得而知其中的硬件奥秘，即：如何错误的识别和如何让其恢复正确的识别，我们只能得知Nvidia的确采用了某种机制来确保dynamic parallelism。\n\n\n### Atomic 和 Shuffle\n\nshfl例子：\n\n```cpp\n#include <cuda_runtime.h>\n#include <device_launch_parameters.h>\n\n__global__ void shflExample(int *data) {\n    int tid = threadIdx.x;\n    int myValue = data[tid];\n\n    // 使用 shfl 进行数据交换，计算累积和\n    for (int offset = 1; offset < blockDim.x; offset *= 2) {\n        int receivedValue = __shfl_sync(0xFFFFFFFF, myValue, tid - offset);\n\n        if (tid >= offset) {\n            myValue += receivedValue;\n        }\n    }\n\n    // 在这里，myValue 包含了线程在数组中前面值的累积和\n\n    // 其他的计算和处理...\n}\n\nint main() {\n    // 启动 CUDA 核函数\n    int dataSize = 32;\n    int *d_data;\n    cudaMalloc((void**)&d_data, dataSize * sizeof(int));\n\n    shflExample<<<1, dataSize>>>(d_data);\n\n    cudaFree(d_data);\n\n    return 0;\n}\n```\n这个例子展示如何在线程束内计算累积和。假设你有一个线程束中的一组值，想要计算每个线程在数组中前面值的累积和，每个线程使用 `__shfl_sync` 从其前面的线程获取值，并将其加到自己的值上。通过循环，每个线程最终都会包含数组中前面所有值的累积和。（此例子为chatgpt提供）\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./j.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\natomic例子：\n\n在早期计算多个数字的总和时需要有两次kernel launch（计算partial sum和计算所有partial sum的sum），使用atomic做一次kernel launch就可以\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a6.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a7.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\n\n\n","type":"Mdx","contentDigest":"87568eb992953a2ffbf04b5c7d65201c","owner":"gatsby-plugin-mdx","counter":231},"frontmatter":{"description":"Nvidia / Cuda 55 Partb 1","title":"Nvidia / Cuda 55 Partb 1"},"exports":{},"rawBody":"import HomepageTemplate from \"./Blogpage.js\";\nexport default HomepageTemplate;\n\n\n## <span style={{ fontWeight: 'bold', color: '#0047ab' }}>Last Updated: 10/22/2023</span>\n\n<PageDescription>\n  <span style={{ fontSize: \"1.8rem\", color: \"#ffffff\", fontWeight: \"light\" }}>\n    本笔记内容来自YouTube视频 \"CUDA Part B_ New Features in CUDA 5 on Kepler; Peter Messmer (NVIDIA)\"\n    <br />\n    感兴趣的人可以自行搜索相关内容。\n    <br />\n    Part B这个部分有点难，分成B1，B2更新（未完）。\n  </span>\n</PageDescription>\n\n\n\n5到5.5的改进\n\n- Performance\n- Efficiency\n- Programmability\n\n早期CUDA只作为计算库编写并没有同时兼顾第一点和第三点，这些问题在后续得到了改进\n\n### Libaray Calls With Kernel \n\n- Libaray calls from kernels，早期不支持内核级系统调用，只支持用`__device__`关键字将函数标记\n- Simpify GPU / CPU devide，整合二者之间分开的地址空间（zero-copy使得cpu和gpu共享相同的物理内存，使用了unified virtual memory）\n```cpp\n#include <iostream>\n\n__global__ void kernel(int* data) {\n    int tid = threadIdx.x;\n    data[tid] = data[tid] * 2;  // 在GPU上执行操作，修改数据\n}\n\nint main() {\n    const int arraySize = 256;\n    int* hostData;\n    int* deviceData;\n\n    // 在主机上分配内存\n    hostData = new int[arraySize];\n\n    // 在设备上分配内存，这里使用cudaMallocManaged分配UVM内存\n    cudaMallocManaged(&deviceData, arraySize * sizeof(int));\n\n    // 初始化数据\n    for (int i = 0; i < arraySize; i++) {\n        hostData[i] = i;\n    }\n\n    // 不需要显式cudaMemcpy\n    kernel<<<1, arraySize>>>(deviceData);\n    cudaDeviceSynchronize();\n\n    // 打印修改后的数据\n    for (int i = 0; i < arraySize; i++) {\n        std::cout << deviceData[i] << \" \";\n    }\n\n    // 释放内存\n    delete[] hostData;\n    cudaFree(deviceData);\n\n    return 0;\n}\n```\n### Dynamic Parallelism\n\n参见[这个slide](https://www.cs.ucr.edu/~amazl001/teaching/cs147/S21/slides/14-DynamicParallelism.pdf)\n\n根据第四页所显示的，当gpu不能自产生任务情况，会加重PCI express bus的传输。\n\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./c.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./d.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n第十一页中模拟了一个尾流的计算情况，众所周知，头部的渲染实际上是比较容易预测的，\n但是没有Dynamic Parallelism的显卡不具备将重要任务和次要任务分配不同资源的能力。\n\n一些更具体的例子可以看[这个视频](https://www.youtube.com/watch?v=X74BLPO8tT4)\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./e.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./f.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\ncuda允许我们在父kernel里调用子kernel就是Dynamic Parallelism的体现，在开普勒系列之前，子kernel的调用要参照上述图所写的形式（参考费米系列）\n\n另外一种更明显的例子就是在面对序列化的数据时、或非uniform的邻接矩阵/链表时\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./h.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./i.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n很明显右图的例子是不可取的，我们应该使用左图这种方式。\n\n### Hyper-Q 和 Grid Management Unit\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![第4页](./a1.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n上图为GK110新增的硬件处理单元“Grid Management Unit”，Grid Management Unit的存在是动态并行机制的核心，因为SMX会生成一些cuda generated works（kernel）返回GMU内，在pending grid中等待，而左图旧版本的GPU缺乏生成子任务并返回调度的机制。\n\n参考材料[Hyper Q Sample](https://developer.download.nvidia.com/compute/DevZone/C/html_x64/6_Advanced/simpleHyperQ/doc/HyperQ.pdf) 所给出的例子，Hyper-Q和GMU诞生的原因并非CPU产生的任务序列们不能并行的执行在GPU，而是有另外一种意想不到的情况发生：\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a4.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a3.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n如图所示，A-B-C是一个需要有序执行的、原本上可以在GPU并行的大任务序列，CPU产生了这个任务，并将其分割成了A0-B0-C0、A1-B1-C1、A2-B2-C2，三个可供GPU并行执行的小任务，但是硬件并不能正确的理解三者并行的概念。\n\n硬件实际只能并行执行C0-A1和C1-A2，因为硬件只能识别两组子任务的交界处来判断这是两组不同的子任务。\n\n为了验证这个猜想，以下用Visual Profiler来模拟：\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a5.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a2.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n可以看到，在实际操作中，不加GMU的GPU确实没有办法理解整体并行的概念，而是只能理解交界处的任务可以共同执行，加了GMU可以做到真正的并行。我们不得而知其中的硬件奥秘，即：如何错误的识别和如何让其恢复正确的识别，我们只能得知Nvidia的确采用了某种机制来确保dynamic parallelism。\n\n\n### Atomic 和 Shuffle\n\nshfl例子：\n\n```cpp\n#include <cuda_runtime.h>\n#include <device_launch_parameters.h>\n\n__global__ void shflExample(int *data) {\n    int tid = threadIdx.x;\n    int myValue = data[tid];\n\n    // 使用 shfl 进行数据交换，计算累积和\n    for (int offset = 1; offset < blockDim.x; offset *= 2) {\n        int receivedValue = __shfl_sync(0xFFFFFFFF, myValue, tid - offset);\n\n        if (tid >= offset) {\n            myValue += receivedValue;\n        }\n    }\n\n    // 在这里，myValue 包含了线程在数组中前面值的累积和\n\n    // 其他的计算和处理...\n}\n\nint main() {\n    // 启动 CUDA 核函数\n    int dataSize = 32;\n    int *d_data;\n    cudaMalloc((void**)&d_data, dataSize * sizeof(int));\n\n    shflExample<<<1, dataSize>>>(d_data);\n\n    cudaFree(d_data);\n\n    return 0;\n}\n```\n这个例子展示如何在线程束内计算累积和。假设你有一个线程束中的一组值，想要计算每个线程在数组中前面值的累积和，每个线程使用 `__shfl_sync` 从其前面的线程获取值，并将其加到自己的值上。通过循环，每个线程最终都会包含数组中前面所有值的累积和。（此例子为chatgpt提供）\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./j.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\natomic例子：\n\n在早期计算多个数字的总和时需要有两次kernel launch（计算partial sum和计算所有partial sum的sum），使用atomic做一次kernel launch就可以\n\n<ImageGallery>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a6.png)\n\n</ImageGalleryImage>\n\n<ImageGalleryImage alt=\"\" title=\"\" col={4}>\n\n![](./a7.png)\n\n</ImageGalleryImage>\n\n</ImageGallery>\n\n\n\n\n","fileAbsolutePath":"/root/site/blue-phoenix/src/pages/nvidia/cuda55partb1/index.mdx"}}},
    "staticQueryHashes": ["1364590287","137577622","2102389209","2456312558","2746626797","3018647132","3037994772","768070550"]}